
#include <cmap.h>
#include <hash.h>
#include <permutations.h>

/**
 * @brief runs and reports correctness tests on the cmap implementation
 * @return true if all the tests passed, false otherwise
 */
bool cmap_correctness_test();
